interface Settings {
  darkMode: boolean;
  fontSize: number;
  language: string;
}

export const settings: Settings = {
  darkMode: true,
  fontSize: 16,
  language: "en",
};

// type Settings2 = {
//   darkMode: boolean;
//   fontSize: number;
//   language: string;
// };
